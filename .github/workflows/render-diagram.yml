name: Render Mermaid Diagram

on:
  push:
    paths:
      - "diagrams/architecture.mmd"
  workflow_dispatch:

jobs:
  render:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - name: Install mermaid-cli
        run: npm install -g @mermaid-js/mermaid-cli

      - name: Write Puppeteer no-sandbox config
        run: |
          echo '{"args": ["--no-sandbox", "--disable-setuid-sandbox"]}' > puppeteer-config.json

      - name: Render SVG and PNG
        run: |
          mmdc -p puppeteer-config.json -i diagrams/architecture.mmd -o diagrams/architecture.svg -b transparent
          mmdc -p puppeteer-config.json -i diagrams/architecture.mmd -o diagrams/architecture.png -b white

      - name: Commit rendered diagram
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add diagrams/architecture.svg diagrams/architecture.png
          git diff --staged --quiet || git commit -m "Auto-render architecture diagram [skip ci]"
          git push
