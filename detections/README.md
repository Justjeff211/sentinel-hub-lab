# Detections

Sentinel analytics rules and detection logic developed against telemetry from the lab.

Not started yet — this fills in once Sentinel is configured and the lab's generating real activity to detect.
